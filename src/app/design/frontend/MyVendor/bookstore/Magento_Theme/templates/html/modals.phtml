<?php
/**
 * Pop-up và Sidebar cho Bookstore
 * @var \Magento\Framework\View\Element\Template $block
 */
?>

<div class="modal-overlay" id="authModal">
    <div class="modal-content auth-modal">
        <button type="button" class="modal-close" onclick="closeModal()">&times;</button>
        
        <div class="auth-tabs">
            <button type="button" class="tab-btn active" id="loginTab" onclick="switchAuth('login')">
                <?= $block->escapeHtml(__('Đăng nhập')) ?>
            </button>
            <button type="button" class="tab-btn" id="registerTab" onclick="switchAuth('register')">
                <?= $block->escapeHtml(__('Đăng ký')) ?>
            </button>
        </div>

        <div id="loginView" class="auth-form-container active">
            <form class="auth-form" action="<?= $block->getUrl('customer/account/loginPost') ?>" method="post">
                <?= $block->getBlockHtml('formkey') ?>
                <div class="form-group">
                    <label><?= $block->escapeHtml(__('Email')) ?></label>
                    <input type="email" name="login[username]" placeholder="name@example.com" required>
                </div>
                <div class="form-group">
                    <label><?= $block->escapeHtml(__('Mật khẩu')) ?></label>
                    <input type="password" name="login[password]" placeholder="••••••••" required>
                </div>
                <div class="form-footer">
                    <a href="<?= $block->getUrl('customer/account/forgotpassword') ?>"><?= $block->escapeHtml(__('Quên mật khẩu?')) ?></a>
                </div>
                <button type="submit" class="btn-auth-submit"><?= $block->escapeHtml(__('Đăng nhập ngay')) ?></button>
            </form>
        </div>

        <div id="registerView" class="auth-form-container">
            <form class="auth-form" action="<?= $block->getUrl('customer/account/createpost') ?>" method="post">
                <?= $block->getBlockHtml('formkey') ?>
                <div class="form-grid">
                    <div class="form-group">
                        <label><?= $block->escapeHtml(__('Họ')) ?></label>
                        <input type="text" name="lastname" placeholder="<?= $block->escapeHtml(__('Nhập họ...')) ?>" required>
                    </div>
                    <div class="form-group">
                        <label><?= $block->escapeHtml(__('Tên')) ?></label>
                        <input type="text" name="firstname" placeholder="<?= $block->escapeHtml(__('Nhập tên...')) ?>" required>
                    </div>
                    <div class="form-group">
                        <label><?= $block->escapeHtml(__('Tên người dùng')) ?></label>
                        <input type="text" name="middlename" placeholder="<?= $block->escapeHtml(__('Tên hiển thị')) ?>">
                    </div>
                    <div class="form-group">
                        <label><?= $block->escapeHtml(__('Số điện thoại')) ?></label>
                        <input type="tel" name="telephone" placeholder="0xxxxxxxxx">
                    </div>
                    <div class="form-group full">
                        <label><?= $block->escapeHtml(__('Email')) ?></label>
                        <input type="email" name="email" placeholder="name@example.com" required>
                    </div>
                    <div class="form-group">
                        <label><?= $block->escapeHtml(__('Mật khẩu')) ?></label>
                        <input type="password" name="password" placeholder="<?= $block->escapeHtml(__('Tối thiểu 8 ký tự')) ?>" required>
                    </div>
                    <div class="form-group">
                        <label><?= $block->escapeHtml(__('Xác nhận mật khẩu')) ?></label>
                        <input type="password" name="password_confirmation" id="password-confirmation" placeholder="<?= $block->escapeHtml(__('Nhập lại mật khẩu')) ?>" required>
                    </div>
                    <div class="form-group full">
                        <label><?= $block->escapeHtml(__('Địa chỉ giao hàng')) ?></label>
                        <input type="text" name="street[]" placeholder="<?= $block->escapeHtml(__('Số nhà, tên đường')) ?>">
                        <input type="text" name="city" placeholder="<?= $block->escapeHtml(__('Thành phố')) ?>">
                        <input type="text" name="postcode" placeholder="<?= $block->escapeHtml(__('Mã bưu chính')) ?>">
                        <select name="country_id">
                            <option value="VN">Vietnam</option>
                            <option value="US">United States</option>
                        </select>
                    </div>
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn-auth-submit"><?= $block->escapeHtml(__('Tạo tài khoản')) ?></button>
                </div>
            </form>
        </div>

        <div class="auth-divider">
            <span><?= $block->escapeHtml(__('hoặc tiếp tục với')) ?></span>
        </div>

        <button type="button" class="btn-google" onclick="window.location.href='<?= $block->escapeUrl($block->getUrl('shop/auth/google')) ?>'">
            <img src="https://www.gstatic.com/firebasejs/ui/2.0.0/images/auth/google.svg" alt="Google">
            <?= $block->escapeHtml(__('Tiếp tục với Google')) ?>
        </button>
    </div>
</div>

<div id="cart-sidebar" class="sidebar-wrapper">
    <div class="sidebar-content">
        <div class="sidebar-header">
            <h3><?= $block->escapeHtml(__('Giỏ hàng của bạn (0)')) ?></h3>
            <button type="button" class="btn-close-sidebar" onclick="toggleCart(false)">&times;</button>
        </div>
        <div class="sidebar-body">
            <div class="empty-cart">
                <div class="empty-icon">🛒</div>
                <p><?= $block->escapeHtml(__('Giỏ hàng của bạn đang trống')) ?></p>
                <button type="button" class="btn-continue" onclick="toggleCart(false)">
                    <?= $block->escapeHtml(__('Tiếp tục mua sắm')) ?>
                </button>
            </div>
        </div>
        <div class="sidebar-footer">
            <div class="cart-total">
                <span><?= $block->escapeHtml(__('Tổng cộng:')) ?></span>
                <strong>0đ</strong>
            </div>
            <a href="<?= $block->getUrl('checkout') ?>" class="btn-checkout">
                <?= $block->escapeHtml(__('Thanh toán ngay')) ?>
            </a>
        </div>
    </div>
</div>

<div class="sidebar-overlay" onclick="toggleCart(false)"></div>

<script>
    // Logic Switch Tab
    window.switchAuth = function(type) {
        const loginView = document.getElementById('loginView');
        const registerView = document.getElementById('registerView');
        const loginTab = document.getElementById('loginTab');
        const registerTab = document.getElementById('registerTab');

        if (type === 'login') {
            loginView.classList.add('active');
            registerView.classList.remove('active');
            loginTab.classList.add('active');
            registerTab.classList.remove('active');
        } else {
            registerView.classList.add('active');
            loginView.classList.remove('active');
            registerTab.classList.add('active');
            loginTab.classList.remove('active');
        }
    };

    // Logic Modal (Dùng class .active khớp với _extend.less)
    window.openModal = function(type) {
        const modal = document.getElementById('authModal');
        if (modal) {
            modal.classList.add('active');
            window.switchAuth(type);
            document.body.style.overflow = 'hidden';
        }
    };

    window.closeModal = function() {
        const modal = document.getElementById('authModal');
        if (modal) {
            modal.classList.remove('active');
            document.body.style.overflow = '';
        }
    };

    window.toggleCart = function(isOpen) {
        const cart = document.getElementById('cart-sidebar');
        if (cart) {
            isOpen ? cart.classList.add('active') : cart.classList.remove('active');
            document.body.style.overflow = isOpen ? 'hidden' : '';
        }
    };

    // Magento yêu cầu Confirm Password, mình sync tự động để User đỡ phải nhập 2 lần
    const passInput = document.querySelector('input[name="password"]');
    const confirmInput = document.getElementById('password-confirmation');
    if (passInput && confirmInput) {
        passInput.addEventListener('input', (e) => confirmInput.value = e.target.value);
    }
</script>

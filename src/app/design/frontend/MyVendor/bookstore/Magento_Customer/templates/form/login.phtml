<?php
/** @var \Magento\Customer\Block\Form\Login $block */
/** @var $escaper \Magento\Framework\Escaper */
?>
<div class="login-container-wrapper">
    <div class="login-card">
        <div class="login-header">
            <h2><?= $escaper->escapeHtml(__('Đăng nhập')) ?></h2>
            <p><?= $escaper->escapeHtml(__('Mừng bạn quay trở lại với BookStore')) ?></p>
        </div>

        <div class="block-content">
            <form class="form form-login form-login-custom"
                  action="<?= $escaper->escapeUrl($block->getPostActionUrl()) ?>"
                  method="post"
                  id="login-form"
                  data-mage-init='{"validation":{}}'>
                <?= $block->getBlockHtml('formkey') ?>
                
                <fieldset class="fieldset-custom-login">
                    <div class="field-group-custom required">
                        <label class="label-custom" for="email"><span><?= $escaper->escapeHtml(__('Email')) ?></span></label>
                        <div class="control-custom">
                            <input name="login[username]" 
                                   value="<?= $escaper->escapeHtmlAttr($block->getUsername()) ?>"
                                   id="email" type="email" class="input-custom-field"
                                   placeholder="name@example.com"
                                   data-validate="{required:true, 'validate-email':true}">
                        </div>
                    </div>

                    <div class="field-group-custom required">
                        <label for="pass" class="label-custom"><span><?= $escaper->escapeHtml(__('Mật khẩu')) ?></span></label>
                        <div class="control-custom">
                            <input name="login[password]" type="password" class="input-custom-field" id="pass"
                                   placeholder="••••••••"
                                   data-validate="{required:true}">
                        </div>
                    </div>
                </fieldset>

                <div class="login-actions-center-block">
                    <button type="submit" class="btn-login-submit-custom" name="send" id="send2">
                        <span><?= $escaper->escapeHtml(__('Đăng nhập ngay')) ?></span>
                    </button>
                    
                    <a class="btn-forgot-password-red" href="<?= $escaper->escapeUrl($block->getForgotPasswordUrl()) ?>">
                        <span><?= $escaper->escapeHtml(__('Quên mật khẩu?')) ?></span>
                    </a>
                </div>
            </form>

            <div class="social-divider-custom">
                <span><?= $escaper->escapeHtml(__('hoặc tiếp tục với')) ?></span>
            </div>

            <div class="social-actions-custom">
                <button type="button" class="btn-google-login-custom" onclick="window.location.href='#'">
                    <img src="https://www.gstatic.com/firebasejs/ui/2.0.0/images/auth/google.svg" alt="Google">
                    <span><?= $escaper->escapeHtml(__('Google')) ?></span>
                </button>
            </div>

            <div class="login-footer-left-align">
                <p>
                    <?= $escaper->escapeHtml(__('Chưa có tài khoản?')) ?> 
                    <a href="<?= $block->getUrl('customer/account/create') ?>" class="action-register">
                        <strong><?= $escaper->escapeHtml(__('Đăng ký ngay')) ?></strong>
                    </a>
                </p>
            </div>
        </div>
    </div>
</div>

<script type="text/x-magento-init">
    {
        "*": {
            "Magento_Customer/js/block-submit-on-send": {
                "formId": "login-form"
            }
        }
    }
</script>
<?php
/**
 * @var \Magento\Customer\Block\Address\Book $block
 * @var \Magento\Framework\Escaper $escaper
 */
$defaultShipping = $block->getDefaultShipping();
?>
<a class="account-dashboard-back-link" href="<?= $escaper->escapeUrl($block->getUrl('customer/account/')) ?>">
    <?= $escaper->escapeHtml(__('Quay lại trang tổng quan thông tin')) ?>
</a>
<div class="address-book-grid address-book-grid-single">
    <div class="address-book-card">
        <div class="address-book-card-header">
            <div class="address-book-card-title">
                <span class="ab-icon ab-shipping">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/>
                        <circle cx="12" cy="10" r="3"/>
                    </svg>
                </span>
                Địa chỉ giao hàng mặc định
            </div>
            <?php if ($defaultShipping): ?>
                <a class="account-card-edit address-book-edit-button" href="<?= $escaper->escapeUrl($block->getAddressEditUrl($defaultShipping)) ?>">Sửa</a>
            <?php endif; ?>
        </div>
        <div class="address-book-card-body">
            <?php if ($defaultShipping): ?>
                <address>
                    <?= $block->getAddressHtml($block->getAddressById($defaultShipping)) ?>
                </address>
            <?php else: ?>
                <div class="ab-empty">Bạn chưa có địa chỉ giao hàng mặc định.</div>
                <div class="ab-card-actions">
                    <a href="<?= $escaper->escapeUrl($block->getAddAddressUrl()) ?>">Thêm địa chỉ</a>
                </div>
            <?php endif; ?>
        </div>
    </div>
</div>

<?php
/** @var \Magento\Sales\Block\Order\Recent $block */
$_orders = $block->getOrders(); // Lấy danh sách đơn hàng 
$count = count($_orders); // Đếm số lượng đơn 
?>

<div class="bookstore-unified-account-card" style="margin-top: 24px;">
    
    <div class="bookstore-account-card-header" style="display: flex; justify-content: space-between; align-items: center;">
        <strong>Đơn hàng gần đây</strong>
        <?php if ($count > 0): ?>
            <a class="action-edit-inline" href="<?= $block->escapeUrl($block->getUrl('sales/order/history')) ?>">
                Xem tất cả
            </a>
        <?php endif; ?>
    </div>

    <?php if ($count > 0): ?>
        <div class="table-wrapper orders-recent" style="overflow-x: auto; width: 100%;">
            <table class="data table table-order-items recent" id="my-orders-table" style="width: 100%; border-collapse: collapse; font-size: 14px;">
                <thead>
                    <tr style="border-bottom: 2px solid #e2e8f0; text-align: left;">
                        <th scope="col" style="padding: 12px 8px; color: #64748b; font-weight: 600;">Mã đơn</th>
                        <th scope="col" style="padding: 12px 8px; color: #64748b; font-weight: 600;">Ngày đặt</th>
                        <th scope="col" style="padding: 12px 8px; color: #64748b; font-weight: 600;">Người nhận</th>
                        <th scope="col" style="padding: 12px 8px; color: #64748b; font-weight: 600;">Tổng tiền</th>
                        <th scope="col" style="padding: 12px 8px; color: #64748b; font-weight: 600;">Trạng thái</th>
                        <th scope="col" style="padding: 12px 8px; color: #64748b; font-weight: 600;">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($_orders as $_order): ?>
                        <tr style="border-bottom: 1px solid #f1f5f9; transition: background-color 0.2s ease;">
                            <td style="padding: 12px 8px; font-weight: 700; color: #0f172a;">
                                <?= $block->escapeHtml($_order->getRealOrderId()) ?>
                            </td>
                            <td style="padding: 12px 8px; color: #334155;">
                                <?= $block->escapeHtml($block->formatDate($_order->getCreatedAt())) ?>
                            </td>
                            <td style="padding: 12px 8px; color: #334155;">
                                <?= $_order->getShippingAddress() ? $block->escapeHtml($_order->getShippingAddress()->getName()) : '-' ?>
                            </td>
                            <td style="padding: 12px 8px; font-weight: 700; color: #0f172a;">
                                <?= /* @noEscape */ $_order->formatPrice($_order->getGrandTotal()) ?>
                            </td>
                            <td style="padding: 12px 8px;">
                                <span class="status-label-custom" style="background: #e0f2fe; color: #0369a1; padding: 4px 8px; border-radius: 4px; font-size: 12px; font-weight: 600; display: inline-block;">
                                    <?= $block->escapeHtml(__($_order->getStatusLabel())) ?>
                                </span>
                            </td>
                            <td style="padding: 12px 8px;">
                                <a class="action-edit-inline" href="<?= $block->escapeUrl($block->getViewUrl($_order)) ?>">
                                    Chi tiết
                                </a>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    <?php else: ?>
        <div class="message info empty" style="padding: 20px; background: #f8fafc; border: 1px dashed #cbd5e1; border-radius: 8px; color: #64748b; text-align: center; margin-top: 10px;">
            <span>Bạn chưa có đơn hàng nào hành trình tại hệ thống.</span>
        </div>
    <?php endif; ?>
</div>
